# amap_district_search

## 用途

调用高德行政区域查询接口，查询省、市、区县等行政区域信息。

## 适用场景

- 用户希望查询城市或行政区编码。
- 需要为天气、公交等接口补齐行政区信息。

## 不适用场景

- 用户提供的是详细街道地址，优先使用 `amap_geocode`。

## 调用注意事项

- `keywords` 可填写行政区名称。
- `subdistrict` 控制返回下级行政区层级。
