# amap_coordinate_convert

## 用途

调用高德坐标转换接口，将其他坐标系转换为高德坐标。

## 适用场景

- 用户提供 GPS、百度、地图吧等坐标，需要转换为高德坐标。

## 不适用场景

- 用户提供的是自然语言地址，应该使用 `amap_geocode`。

## 调用注意事项

- `locations` 可以包含多个坐标，使用 `|` 分隔。
- `coordsys` 必须明确。
