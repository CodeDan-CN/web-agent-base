# content_extract

## 用途

从用户提供的文本中提取摘要、关键要点或结构化大纲。

## 适用场景

- 用户提供了明确文本。
- 用户要求总结、提炼、归纳、结构化。
- 用户要求从一段说明中提取重点。

## 不适用场景

- 用户没有提供原始文本。
- 用户要求读取不可访问文件。
- 用户要求基于外部实时数据分析。

## 调用注意事项

- 必须提供 `content`。
- `extract_mode` 可选，支持 `key_points`、`summary`、`outline`。
- Skill 结果只返回结构化数据，不直接面向用户。
