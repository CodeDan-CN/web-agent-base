# amap_geocode

## 用途

调用高德 Web 服务地理编码接口，将结构化地址解析为经纬度、adcode 和 citycode。

## 适用场景

- 用户提供了明确地址。
- 后续 Skill 需要经纬度或 adcode。
- 路径规划、天气查询链路需要先解析地址。

## 不适用场景

- 用户没有提供地址。
- 用户只提供过于模糊的位置描述。

## 调用注意事项

- 必须提供 `address`。
- `city` 可选，但建议提供以提升解析稳定性。
- API Key 由 Runtime 从 `.env` 读取，不写入 Skill 资产。
